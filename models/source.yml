version: 2

sources:
  - name: raw # this is the source_name
    database: development
    schema: dbt_san8

    tables:
      - name: this_raw # this is the table_name
  - name: jaffle_shop # this is the source_name
    database: raw

    tables:
      - name: customers # this is the table_name
      - name: orders